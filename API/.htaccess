<IfModule mod_rewrite.c>
    RewriteEngine On

    # Para las rutas API
    RewriteCond %{REQUEST_URI} ^/api [NC]
    RewriteRule ^ /index.php [L]

    # Para otras rutas web
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [L]
</IfModule>
